# create-productflow-kit

Generate ProductFlow Kit starter applications.

```bash
npm create productflow-kit@latest my-app -- --template ai-saas --data jpa
```

Supported templates:

- `saas-admin`
- `ai-saas`

Supported data layers:

- `jpa` / `jpa-flyway`
- `mybatis` / `mybatis-plus`
